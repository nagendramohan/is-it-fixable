# Optional: a GitHub token raises the API rate limit from 60/hr to 5000/hr.
# A classic token with default (public) scope is enough for public repositories.
# Copy to .env (git-ignored) or export it in your shell.
GITHUB_TOKEN=
